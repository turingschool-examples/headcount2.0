import React from 'react';
import Card from './Card';
import PropTypes from 'prop-types';
import helper from '../helper';

const SelectedCont = ({ data, selectedCont }) => {
  
}

export default SelectedCont;