import React from 'react';
import ReactDOM from 'react-dom';
import CardsDisplay from '../src/CardsDisplay';
import { shallow } from 'enzyme';
import DistrictRepository from './helper.js'
import kinderData from '../data/kindergartners_in_full_day_program.js';


describe('CardsDisplay', () => {

  // it('renders without crashing', () => {
  //   const div = document.createElement('div');
  //   ReactDOM.render(<CardsDisplay />, div);
  // });

  // it('CardsDisplay section has a class of cards-display', () => {
  //   const wrapper = shallow(<CardsDisplay />);
  //
  //   expect(wrapper.find('section').hasClass('cards-display')).toEqual(true);
  // })


})
